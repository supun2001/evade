# evade

## Server

The Colyseus backend lives in [server/README.md](/Volumes/HanzoP/Unity/evade/server/README.md).

For public WebGL deployment, do not rely on GitHub Codespaces forwarded ports. Use a normal public host and point the Unity client at it with:

```text
?server=wss://your-server-hostname.example.com
```
